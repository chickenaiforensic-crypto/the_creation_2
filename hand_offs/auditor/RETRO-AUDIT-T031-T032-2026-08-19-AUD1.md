# RETRO-AUDIT — T-031 (Monte Carlo) · T-032a (Madrid ATP) · T-032b (Madrid WTA) — RESULT: 0 ISSUES

**Date:** 2026-08-19 · **From:** Auditor 1 · **Branch:** arena/01a015bb-the-creation-2
**Order:** Director instruction — apply the pre-audit → work → post-audit standard retroactively to the three shipped slice-sets so the external audit team finds nothing.
**Scope:** `editions/Monte_Carlo_Masters/` (commit `3cfefa9`), `editions/Madrid_Masters/` (commit `34033a2`), `editions/Madrid/` (commit `d839bfd`) — 18 edition files, 1,326 rows, 40 fix rows, plus their MANIFEST entries, gap_report entries (MC001–011, MAM001–021, MA001–008) and closure notes.
**Store baseline:** claude_1 pinned store re-verified md5 `a280b2fb56e64f64724473e767d90485` before the run. Harness: `/tmp/retro_audit.py` (rebuildable from this spec).

---

## Checks performed (per edition file, all 18)

1. **Row hygiene** — exact 23-key schema; winner=="A"; no self-play; initials regex `\b[A-Z]\.` (0 hits); date-starts-with-year; constant fields (tournament/tour/tier/surface=Clay/indoor=false/bestOf=3/defaulted=false); W/O hygiene (score "W/O", zeroed sets/games, status walkover, retired false); retirement status consistency; score-vs-sets/games arithmetic re-derived through the canonical parser for every non-W/O row.
2. **Structure** — era-correct draw_size and round template (56-draw 55/55 ×8-byes; 64-draw 63/63 ×**0-byes**; 96-draw 95/95 ×32-byes); exhaustive spine (no ghosts, no winners-lost, bye exemption only at first present round); no duplicate round-pairs; rows sorted (round, date).
3. **Fidelity vs source-of-truth** — every NON-fix row byte-compared against the claude_1 store row (round, players, score, date, sets, games, flags, status, winner) modulo the two documented J.J. Wolf→Jeffrey Wolf renames: **exact multiset match in all 18 files**. No pulled row was altered, dropped, or duplicated.
4. **Fix-row provenance** — fix rows identified strictly by the `merge` field (`T-031-…`, `T-032A-…`, `T-032B-…`); each carries a TE match-detail URL whose id also appears in its gap_report resolution; all 40 TE ids unique.
5. **Gap ↔ row ↔ manifest crosswalk** — per file: gap_report entries (status closed, folder, file_path) == fix rows in file == manifest `gap_count`; each resolution text matched to an actual row on (winner, round, date, score, TE id); manifest sha256/match_count/draw_size re-hashed and exact.
6. **Closure-note quantitative claims re-derived from bytes:**
   - **T-031 MC:** 11 fixes = 10 ret + 1 W/O ✓; 1 tied-token row (Auger-Aliassime d. Ruud 7-5 2-2) ✓; post-fix byes == 8 ×6 incl. the two LL-into-bye editions ✓.
   - **T-032a MAM:** 21 = 16 ret + 5 W/O ✓; 4 tied-token rows (Sinner–Pella 4-4, FAA–Lehecka 3-3, Fritz–Bonzi 0-0, Moller–Diallo 3-3) ✓; 1 loser-ahead-on-games row (Kopriva d. Rinderknech 6-4 3-6, games 9-10) ✓.
   - **T-032b MA:** 8 = 6 ret + 2 W/O ✓; the three "loser-was-ahead" rows verified at token level (Bouzkova 2-3 token→B, Minnen 0-2 token→B, Swiatek 2-6 set won) — note these are current-set/set-won claims, byte-true as worded ✓.
7. **Summaries** — one per edition file, present for all 18.
8. **Full-branch audit after the retro pass:** EDITIONS: 167, ROWS: 12,651, **ISSUES: 0**; working tree untouched (retro-audit was read-only — zero modifications were needed).

## Harness self-correction (disclosed)

First run of the harness crashed on its own fix-row detector: it matched the substring "T-032a" inside the Wolf-rename `name_fix` note and misclassified 1 pulled row as a fix row. The detector was tightened to the exact `merge`-field prefix and re-run. This was a **harness bug, not a data bug** — final result over the corrected detector: **0 issues**.

## Verdict

T-031, T-032a and T-032b are **error-free under the full retroactive protocol**: every shipped row is either byte-identical to the pinned source or a TE-sourced fix with complete provenance; every structural claim, counter and closure-note figure reproduces from bytes. No corrections were required. The previously-disclosed items (T-032a post-write Wolf catch; the two PLAN-T032 arithmetic slips, plan-only) remain the complete disclosure set for these tasks.

Role: AUDITOR 1 · Branch ID: arena/01a015bb-the-creation-2
