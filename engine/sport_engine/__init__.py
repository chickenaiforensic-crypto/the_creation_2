"""Sport Engine — pluggable sport data processing engine."""

# App version — bump EVERY update (v1.1, v1.2, v1.3, ...). This is the single
# source of truth; it is exposed via the /api/ui manifest and rendered in the
# app header. The version string is the one deliberate code literal (metadata).
__version__ = "v1.2"
