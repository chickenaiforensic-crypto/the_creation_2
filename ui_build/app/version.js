/* Tennis UI — version carrier. Version: v1.4
   Single shipped source of the UI version string; must always equal
   ui_build/VERSION (enforced by the audit harness). Never edit the badge
   text by hand — bump both files together per the version ledger in
   ui_build/PROVENANCE.md §10. */
"use strict";
window.APP_VERSION = "v1.4";
